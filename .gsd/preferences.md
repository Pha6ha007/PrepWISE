# Confide — Verification Commands

## Run before committing
```bash
npx tsc --noEmit        # TypeScript check (0 errors expected)
npm run test:e2e        # Playwright E2E tests (40 tests, ~2.5 min)
```
